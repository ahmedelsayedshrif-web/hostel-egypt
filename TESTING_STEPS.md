# خطوات الاختبار بعد إعادة البناء

## ✅ الخطوات المكتملة
1. ✅ إيقاف التطبيق القديم
2. ✅ إعادة بناء Frontend
3. ⏳ إعادة بناء التطبيق الكامل (Electron) - قيد التنفيذ

## 📋 الخطوات التالية

### 1. فتح التطبيق
```bash
# بعد انتهاء البناء، افتح التطبيق
Start-Process "build-output\win-unpacked\HOSTEL MASR.exe"
```

### 2. حذف البيانات القديمة
- افتح التطبيق
- اذهب إلى صفحة "الإعدادات" (Settings)
- اضغط على "حذف جميع البيانات"
- أكد الحذف

### 3. إضافة بيانات الاختبار
استخدم API endpoint لإضافة البيانات:
```javascript
// في المتصفح Console أو Postman
fetch('http://127.0.0.1:5000/api/data/seed', {
  method: 'POST'
}).then(r => r.json()).then(console.log)
```

أو استخدم script Node.js:
```bash
node -e "fetch('http://127.0.0.1:5000/api/data/seed', {method: 'POST'}).then(r => r.json()).then(console.log)"
```

### 4. التحقق من الأرقام

#### Dashboard
- ✅ افتح صفحة "الرئيسية" (Dashboard)
- ✅ اختر الشهر الحالي (ديسمبر 2025)
- ✅ تحقق من:
  - إجمالي الإيرادات
  - صافي الربح
  - نصيب الشركاء
  - عدد الحجوزات

#### Financial
- ✅ افتح صفحة "المالية" (Financial)
- ✅ اختر نفس الشهر (ديسمبر 2025)
- ✅ تحقق من:
  - إجمالي الإيرادات (يجب أن تطابق Dashboard)
  - صافي الربح (يجب أن يطابق Dashboard)
  - نصيب الشركاء (يجب أن يطابق Dashboard)

### 5. التحقق من الأشهر المختلفة
- ✅ نوفمبر 2025 (3 حجوزات قديمة)
- ✅ ديسمبر 2025 (2 حجوزات نشطة)
- ✅ يناير 2026 (2 حجوزات قادمة)

### 6. التحقق من الحسابات

#### نوفمبر 2025
- BK-PAST-001: $840 → brokerProfit: $22
- BK-PAST-002: $1400 → brokerProfit: $102
- BK-PAST-003: $400 → brokerProfit: $105
- **الإجمالي المتوقع: $229**

#### ديسمبر 2025
- BK-ACTIVE-001: $1080 → brokerProfit: $29
- BK-ACTIVE-002: $2000 → brokerProfit: $149
- **الإجمالي المتوقع: $178**

#### يناير 2026
- BK-UPCOMING-001: $560 → brokerProfit: $64
- BK-UPCOMING-002: $300 → brokerProfit: $50
- **الإجمالي المتوقع: $114**

## 🐛 إذا كانت الأرقام لا تزال خاطئة

1. تحقق من أن التطبيق الجديد يعمل
2. تحقق من أن البيانات تم حذفها وإعادة إضافتها
3. تحقق من أن الشهر المختار صحيح
4. تحقق من Console للأخطاء

## 📝 ملاحظات
- جميع الحسابات تستخدم `checkIn` date للفلترة
- `brokerProfit` يُحسب تلقائياً إذا لم يكن موجوداً
- الأرقام يجب أن تكون متطابقة بين Dashboard و Financial

