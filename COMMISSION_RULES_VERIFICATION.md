# ✅ التحقق من قواعد عمولة النقل وتوقيت العمولة

## 📋 القواعد المطلوبة

### 1️⃣ نقل العمولة حتى لو الغرفة الجديدة مباشرة (Direct)

**السيناريو:**
- العميل جاي من Booking.com على **غرفة A** (15% عمولة = $150)
- العميل ما عجبه الغرفة وانتقل لـ **غرفة B**
- **حتى لو الغرفة B مباشرة (Direct)** - يعني لا عمولة منصة جديدة
- Booking.com **هيتضر** - يعني Booking.com لن يأخذ عمولة من الغرفة الجديدة
- لكن **الـ 15% ($150) يجب أن تُخصم من الغرفة الجديدة** كعمولة نقل

**التحقق:**
- ✅ الكود الحالي يتحقق من أن الحجز الأصلي كان من منصة
- ✅ ينقل العمولة حتى لو الحجز الجديد Direct
- ✅ العمولة تُسجل كـ مصروف (transfer commission expense)

### 2️⃣ توقيت عمولة Booking.com (تقفيلات الشهر)

**السيناريو:**
- حجز من **29-12** إلى **4-1**
- Booking.com **يأخذ العمولة في الشهر الجديد بس** (شهر 1)
- علشان تقفيلات الشهر

**المطلوب:**
- ✅ في شهر ديسمبر: الإيرادات فقط (بدون عمولة)
- ✅ في شهر يناير: الإيرادات + العمولة الكاملة

## 🔍 التحقق من الكود الحالي

### النقطة 1: نقل العمولة
الكود الحالي (السطر 843-849):
```javascript
const isFromPlatform = originalSource !== 'Direct' && originalSource !== 'External';
const hasPlatformCommission = originalBooking.platformCommission && originalBooking.platformCommission > 0;

if (hasPlatformCommission && isFromPlatform) {
  transferCommissionAmount = originalBooking.platformCommission;
}
```

**هذا صحيح!** ✅
- إذا الحجز الأصلي كان من منصة → يتم نقل العمولة
- حتى لو الحجز الجديد Direct → العمولة تتنقل كـ مصروف

### النقطة 2: توقيت العمولة
الكود الحالي (السطر 1572-1577):
```javascript
// Commission: Apply if checkout is in this month or earlier
const commissionStatus = booking.commissionStatus || (checkOut > new Date() ? 'pending' : 'applied');
if (commissionStatus === 'applied' || checkOut <= endDate) {
  totalPlatformCommission += booking.platformCommission || 0;
  totalTransferCommissionExpense += booking.transferCommissionAmount || booking.transferCommissionExpense || 0;
}
```

**المشكلة:**
- الحجوزات تُفلتر حسب checkIn فقط
- لكن يجب أيضاً حساب حجوزات checkout في الشهر (للعمولة)

## ✅ المطلوب

1. ✅ **نقل العمولة**: الكود صحيح - يتم نقل عمولة الحجز الأصلي حتى لو الحجز الجديد Direct
2. ⚠️ **توقيت العمولة**: يجب التأكد من أن العمولة تُخصم فقط في شهر checkout

## 📝 ملاحظات

- الكود الحالي ينقل العمولة بشكل صحيح ✅
- لكن يجب التأكد من توقيت خصم العمولة في شهر checkout فقط للحجوزات عبر الشهور

